<%= form_for(@kind, html: {class: "elegant-aero"}) do |f| %>
  <h1>Editing Kind</h1>
  <% if @kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kind.errors.count, "error") %> prohibited this kind from being saved:</h2>

      <ul>
      <% @kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <% link_to 'Show', @kind, class: "button" %>
    <%= f.submit "Save", class: "button" %>
    <%= link_to 'Back', kinds_path, class: "button" %>
    <%= link_to 'Voltar ao inicio', root_path, class: "button" %>
  </div>
<% end %>
